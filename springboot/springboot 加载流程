


prepareContext  准备容器
setEnvironment 设置环境变量

listeners.contextPrepared(context); 初始化完容器 绑定一些监听器

ConfigurableListableBeanFactory beanFactory 获取到对象工厂 先要创建一个默认的 对象工厂 DefaultListableBeanFactory

registerSingleton 注册一个单例对象

beanFactory.registerSingleton